import React from 'react'

const ProductoConstruido = (props) => {
    return(
        <div>
            {props.nombre}
            {props.categoria}
        </div>	

    )
}

export default ProductoConstruido 